#include <iostream>
using namespace std;

int main() {
    int x, y, length;
    cin >> x >> y >> length;
    int centerX = x + length / 2;
    int centerY = y + length / 2;
    cout << centerX <<endl<< centerY << endl;
    return 0;
}